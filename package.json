{
  "name": "messenger",
  "version": "1.0.0",
  "description": "In this project we setup a enviornment in which we chat with our friends and makes groups",
  "main": "index.js",
  "scripts": {
    "client":"cd client && npm start",
    "server":"cd server && npm run dev",
    "devstart":"concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
